import type { BytemdPlugin,BytemdEditorContext } from 'bytemd'
import { createApp } from 'vue'
import MarkdownEditorEmojiPicker from '@/components/MarkdownEditorEmojiPicker.vue'

/**
 * 表情插件（未实现）
 * @returns 
 */
export function emoji(): BytemdPlugin {



  return {
  }
}

