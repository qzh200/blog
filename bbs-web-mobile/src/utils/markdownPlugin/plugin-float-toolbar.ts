import type { BytemdPlugin } from 'bytemd'
import stickybits from 'stickybits'
import { getScrollParent } from '@vant/use';
/**
 * 浮动工具栏插件（未实现）
 * @returns 
 */
export default function floatToolbar(): BytemdPlugin {
  return {
  }
}

